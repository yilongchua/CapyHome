"""Shared utilities used by both work_agent and plan_agent factories.

Add modules here when a piece of code is genuinely shared between modes. Keep
mode-specific logic in the agent's own package (work_agent, plan_agent).
"""
