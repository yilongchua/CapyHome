services:
  websearch-proxy:
    image: nginx:alpine
    ports:
      - "9000:9000"
    volumes:
      - ./websearch-nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      websearch:
        condition: service_healthy
    networks:
      - capyhome
    restart: unless-stopped

  websearch:
    build:
      context: ${WEBSEARCH_ROOT}
      dockerfile: Dockerfile
    image: capyhome-websearch:local
    environment:
      WEBSEARCH_CONFIG_PATH: /app/config.yaml
      SEARXNG_SECRET_KEY: ${SEARXNG_SECRET_KEY:-}
    volumes:
      - ${WEBSEARCH_ROOT}/output:/app/output
      - ${WEBSEARCH_ROOT}/config.yaml:/app/config.yaml:ro
      - ${WEBSEARCH_ROOT}/searxng-settings.yml:/etc/searxng/settings.yml:ro
      - ${WEBSEARCH_ROOT}/prompt:/app/prompt:ro
    expose:
      - "9000"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:9000/health"]
      interval: 20s
      timeout: 5s
      retries: 5
      start_period: 20s
    networks:
      - capyhome
    restart: unless-stopped

  dashboard-logs:
    image: capyhome-websearch:local
    command: ["python", "/app/main.py", "dashboard-logs", "--output-dir", "/app/output", "--interval-seconds", "10", "--window-seconds", "10800", "--limit", "10", "--format", "table"]
    environment:
      WEBSEARCH_CONFIG_PATH: /app/config.yaml
    volumes:
      - ${WEBSEARCH_ROOT}/output:/app/output
      - ${WEBSEARCH_ROOT}/config.yaml:/app/config.yaml:ro
    depends_on:
      websearch:
        condition: service_healthy
    networks:
      - capyhome
    restart: unless-stopped
